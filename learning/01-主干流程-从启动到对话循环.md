# Claude Code 主干流程：从启动到对话循环

> 阅读本文档后，你将理解：用户输入一句话 → Claude 收到并回复 → 如果回复中包含工具调用则执行工具 → 把结果再发给 Claude → 直到没有工具调用为止，这条完整链路。

---

## 一、全局架构图

```mermaid
flowchart TD
    Input["用户终端输入: 帮我写个函数"]

    subgraph cli["cli.tsx — 入口"]
        direction TB
        C1["polyfill feature() → false"]
        C2["注入版本号等全局常量"]
        C3["判断 --version 等快速路径"]
        C1 --> C2 --> C3
    end

    subgraph main["main.tsx — 司令部"]
        direction TB
        M1["解析命令行参数 (Commander)"]
        M2["初始化: 认证、分析、配置"]
        M3["加载 MCP、Skills、权限"]
        M4["构建 system prompt"]
        M5["启动 REPL (launchRepl)"]
        M1 --> M2 --> M3 --> M4 --> M5
    end

    subgraph repl["REPL.tsx — 交互界面"]
        R1["等待用户输入"]
        R2["调用 query()"]
        R3["显示结果"]
        R1 --> R2 --> R3 --> R1
    end

    subgraph query["query.ts — 对话循环引擎 ★ 核心"]
        direction TB
        Q1["准备消息（压缩、去重、附加上下文）"]
        Q2["callModel() 调用 Claude API（流式）"]
        Q3{"有 tool_use?"}
        Q4["执行 tool → 把结果拼入消息"]
        Q1 --> Q2 --> Q3
        Q3 -- "无" --> Q5["返回，本轮结束"]
        Q3 -- "有" --> Q4 --> Q1
    end

    subgraph api["claude.ts — API 客户端"]
        A1["构建请求参数"]
        A2["调 Anthropic SDK"]
        A3["流式返回事件"]
        A1 --> A2 --> A3
    end

    Input --> cli --> main --> repl --> query
    Q2 -.->|"实际调用"| api

    style query fill:#4A90D9,color:#fff
    style cli fill:#2C3E50,color:#fff
    style main fill:#2C3E50,color:#fff
    style repl fill:#2C3E50,color:#fff
    style api fill:#8E44AD,color:#fff
```

---

## 二、启动阶段详解（cli.tsx → main.tsx）

### 2.1 cli.tsx — 门卫

**类比 Java**：相当于 `public static void main(String[] args)`，但做了更多快速路径判断。

**核心逻辑**：
1. 注入全局常量（`MACRO.VERSION`、`BUILD_TARGET` 等）—— 相当于 Java 的 `static final` 常量
2. `feature()` 函数永远返回 `false` —— 所有被 `feature('XXX')` 包裹的代码都是**死代码**，构建时会被消除
3. 判断命令行参数：
   - `--version` → 直接打印版本号，**零模块加载**（最快路径）
   - `--dump-system-prompt` → 输出 system prompt（内部调试用）
   - 其他 → 动态 `import("../main.tsx")` 加载完整 CLI

**Java 对比**：

```mermaid
flowchart LR
    A["Java: main()"] --> B["SpringApplication.run()"]
    C["cli.tsx: main()"] --> D["import main.tsx"] --> E["cliMain()"]

    style A fill:#E8D44D,color:#000
    style C fill:#4A90D9,color:#fff
```

### 2.2 main.tsx — 司令部

**类比 Java**：相当于 Spring Boot 的启动类 + 配置中心。

```mermaid
flowchart TB
    subgraph startup["main.tsx 启动流程"]
        direction TB
        S1["1. 启动 MDM 预读 + Keychain 预取（并行）"]
        S2["2. 解析命令行参数（Commander.js）"]
        S3["3. 初始化 GrowthBook（功能开关）"]
        S4["4. 加载配置、策略限制"]
        S5["5. 加载 MCP 服务器配置"]
        S6["6. 加载插件和 Skills"]
        S7["7. 初始化权限系统"]
        S8["8. 构建 system prompt"]
        S9["9. 启动 REPL"]
        S1 --> S2 --> S3 --> S4 --> S5 --> S6 --> S7 --> S8 --> S9
    end

    subgraph java["Java 等价"]
        direction TB
        J1["@Async 初始化"]
        J2["picocli / args4j"]
        J3["FeatureFlag 服务"]
        J4["application.yml"]
        J5["SPI 服务发现"]
        J6["ServiceLoader"]
        J7["Spring Security"]
        J8["配置模板拼装"]
        J9["Application.run()"]
        J1 --> J2 --> J3 --> J4 --> J5 --> J6 --> J7 --> J8 --> J9
    end

    S1 -.-> J1
    S2 -.-> J2
    S3 -.-> J3

    style startup fill:#2C3E50,color:#fff
    style java fill:#E8D44D,color:#000
```

---

## 三、对话循环详解（query.ts）★ 最重要

这是整个 harness 的**心脏**。理解了它，你就理解了 agent 的本质。

### 3.1 核心循环流程图

```mermaid
flowchart TB
    Start(["用户输入消息"]) --> Init["query() 入口: 初始化 State"]

    Init --> Loop

    subgraph loop["while(true) 循环"]
        direction TB
        S1["1. 消息预处理"]
        S2["2. 调用 Claude API (流式)"]
        S3["3. 检查是否中断 (Ctrl+C)"]
        S4{"4. 有 tool_use?"}
        S5a["5a. 检查恢复逻辑\n(prompt too long / 输出截断)"]
        S5b["5b. 执行工具 runTools()\n收集工具结果"]
        S6a(["6a. 返回，本轮结束"])
        S6b["6b. 拼装消息:\n原消息 + assistant + tool结果"]

        S1 --> S2 --> S3 --> S4
        S4 -- "无" --> S5a --> S6a
        S4 -- "有" --> S5b --> S6b
    end

    S6b -->|"continue 回到循环顶部"| S1

    subgraph preprocess["消息预处理管道"]
        direction LR
        P1["工具结果裁剪"] --> P2["snip 历史裁剪"] --> P3["microcompact"] --> P4["context collapse"] --> P5["autocompact"]
    end

    S1 -.- preprocess

    style Loop fill:#1a1a2e,color:#fff
    style S6a fill:#27AE60,color:#fff
    style S4 fill:#E67E22,color:#fff
    style preprocess fill:#34495E,color:#fff
```

### 3.2 关键概念解释（Java 程序员视角）

#### State — 循环状态

```typescript
type State = {
  messages: Message[]              // 对话历史（相当于 List<ChatMessage>）
  toolUseContext: ToolUseContext    // 工具执行上下文（相当于 ExecutionContext）
  autoCompactTracking: ...         // 压缩追踪状态
  maxOutputTokensRecoveryCount: number  // 输出截断恢复次数
  turnCount: number                // 当前轮次
  transition: Continue | undefined // 为什么继续循环（调试用）
}
```

**类比 Java**：这就是一个 `while` 循环里的局部变量，只不过被提取成了一个对象方便在 `continue` 时传递。

#### 消息预处理管道

在每次调 API 之前，消息会经过一系列处理：

```mermaid
flowchart LR
    Raw["原始消息"] --> P1["applyToolResultBudget\n裁剪过大的工具结果"]
    P1 --> P2["snipCompactIfNeeded\n截断过早的历史消息"]
    P2 --> P3["microcompact\n微压缩: 删除已缓存结果"]
    P3 --> P4["applyCollapsesIfNeeded\n上下文折叠"]
    P4 --> P5["autocompact\n自动压缩: 用小模型总结"]
    P5 --> Ready["准备好的消息"]

    style Raw fill:#7F8C8D,color:#fff
    style Ready fill:#27AE60,color:#fff
```

**类比 Java**：就像 HTTP 请求经过 Filter Chain 一样，每个处理器做一件事。

#### tool_use 块 — 工具调用请求

当 Claude 回复中包含 `tool_use` 块时，表示 Claude 想调用一个工具：

```json
{
  "type": "tool_use",
  "id": "toolu_abc123",
  "name": "Bash",
  "input": { "command": "ls -la" }
}
```

**类比 Java**：这就像 RPC 请求——Claude 是客户端，harness 是服务端，`tool_use` 就是一个 RPC 调用。

#### tool_result — 工具执行结果

harness 执行完工具后，把结果封装成 `tool_result`：

```json
{
  "type": "tool_result",
  "tool_use_id": "toolu_abc123",
  "content": "total 48\ndrwxr-xr-x  6 user staff  192 ..."
}
```

然后把 `[原消息 + assistant回复 + tool_result]` 一起再发给 Claude，进入下一轮循环。

### 3.3 恢复机制（容错设计）

```mermaid
flowchart TB
    Error{"错误类型?"}

    E1["prompt too long\n上下文超长"]
    E2["max_output_tokens\n输出截断"]
    E3["模型过载"]
    E4["stop hook 阻止"]

    R1a["context collapse drain"]
    R1b["reactive compact"]
    R1c["报错"]

    R2a["升级 token 上限到 64k"]
    R2b["注入 继续 提示词 (最多3次)"]

    R3["切换备用模型"]
    R4["注入错误消息后重新循环"]

    Error --> E1 --> R1a
    R1a -- "失败" --> R1b
    R1b -- "失败" --> R1c

    Error --> E2 --> R2a
    R2a -- "仍截断" --> R2b

    Error --> E3 --> R3
    Error --> E4 --> R4

    style Error fill:#E74C3C,color:#fff
    style R1c fill:#E74C3C,color:#fff
```

---

## 四、API 调用层（claude.ts）

### 4.1 请求构建流程

query.ts 中的 `deps.callModel()` 调用 claude.ts 的流式 API 方法，以下是 claude.ts 内部做的事：

```mermaid
flowchart TB
    S1["getSystemPrompt()"] --> Result["完整请求"]
    S2["prependUserContext()\n在用户消息前注入上下文"] --> Result
    S3["appendSystemContext()\n在 system prompt 后附加系统上下文"] --> Result
    S4["toolToAPISchema()\nTool 对象 → JSON Schema"] --> Result
    S5["normalizeMessagesForAPI()\n消息格式标准化"] --> Result

    Result --> SDK["Anthropic SDK streaming call"]

    SDK --> E1["content_block_delta → 文本增量"]
    SDK --> E2["content_block_start → tool_use 开始"]
    SDK --> E3["message_delta → 消息结束"]
    SDK --> E4["message_stop → 流结束"]

    style SDK fill:#8E44AD,color:#fff
```

---

## 五、完整时序图（一次用户提问的生命周期）

```mermaid
sequenceDiagram
    participant U as 用户
    participant R as REPL
    participant Q as query()
    participant A as claude.ts
    participant API as Claude API
    participant T as BashTool

    U->>R: "列出文件"
    R->>Q: query(messages)
    Q->>Q: 预处理消息（压缩管道）
    Q->>A: callModel(messages, systemPrompt, tools)
    A->>API: Anthropic SDK streaming

    API-->>A: tool_use: Bash("ls -la")
    A-->>Q: 收集 assistant 消息 + tool_use 块

    Q->>Q: needsFollowUp = true
    Q->>T: runTools() → BashTool.call("ls -la")
    T->>T: 执行 ls -la
    T-->>Q: tool_result: 文件列表

    Q->>Q: 拼装消息: [user + assistant + tool_result]
    Q->>Q: continue → 回到 while 循环顶部

    Q->>A: callModel(拼装后的消息)
    A->>API: Anthropic SDK streaming
    API-->>A: 纯文本回复（无 tool_use）
    A-->>Q: 收集 assistant 消息

    Q->>Q: needsFollowUp = false → return
    Q-->>R: 最终结果
    R-->>U: 显示: "以下是当前目录的文件列表..."
```

---

## 六、核心数据结构速查

### Message 类型层次

```mermaid
classDiagram
    class Message {
        <<union type>>
    }
    class UserMessage {
        +type: "user"
        +content: string | ContentBlock[]
        +toolUseResult?: string
    }
    class AssistantMessage {
        +type: "assistant"
        +message: API response
        +isApiErrorMessage?: boolean
    }
    class SystemMessage {
        +type: "system"
        +content: string
        +level: "info" | "warning"
    }
    class AttachmentMessage {
        +type: "attachment"
        +attachment: Attachment
    }
    class TombstoneMessage {
        +type: "tombstone"
        +message: Message
    }

    Message <|-- UserMessage
    Message <|-- AssistantMessage
    Message <|-- SystemMessage
    Message <|-- AttachmentMessage
    Message <|-- TombstoneMessage
```

### ToolUseContext — 工具执行上下文

```mermaid
classDiagram
    class ToolUseContext {
        +options: ToolOptions
        +abortController: AbortController
        +readFileState: FileStateCache
        +getAppState() AppState
        +setAppState(fn)
        +messages: Message[]
        +agentId?: string
        +setToolJSX?: Function
        +addNotification?: Function
        +updateFileHistoryState(fn)
        +updateAttributionState(fn)
    }
    class ToolOptions {
        +tools: Tool[]
        +mainLoopModel: string
        +mcpClients: MCPConnection[]
        +agentDefinitions: AgentDefinitions
        +debug: boolean
        +verbose: boolean
        +thinkingConfig: ThinkingConfig
    }
    ToolUseContext --> ToolOptions
```

---

## 七、一句话总结

**Claude Code 的核心就是一个 while 循环**：发消息给 Claude → 如果 Claude 想用工具就执行工具 → 把结果拼回去再发 → 直到 Claude 说"我做完了"。所有其他功能（压缩、权限、MCP、Skills）都是这个循环的增强。

---

## 八、下一步

理解了主干流程后，Day 2 我们将深入四大模块：
- **Tool 系统**：工具是怎么定义、注册、过滤、执行的
- **Prompt 工程**：system prompt 是怎么拼装的
- **Skill 系统**：skill 和 tool 的区别是什么
- **MCP 协议**：外部工具是怎么接入的
